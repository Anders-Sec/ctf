# Secrets & local config
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
coverage/
.vite/

# Editors / OS
.DS_Store
.idea/
*.swp
.venv/
uv.lock.bak

# Load-test seeded sessions carry real session tokens — never commit.
loadtest/sessions.csv
loadtest/.venv/

# The authoring spreadsheet carries real flag values — never commit.
challenge-template.csv

# Solve guide for the System AI ladder. Spoilers — never commit.
SOLVES.md

# Challenge authoring workspace — carries real flags and spoilers. Never commit.
Challenge/
