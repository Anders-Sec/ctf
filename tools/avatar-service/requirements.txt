# Everything except torch, which needs its own index (see README.md) because the
# wheel has to match your GPU's compute capability.
diffusers>=0.31
transformers>=4.44
accelerate>=1.0
safetensors>=0.4
fastapi>=0.115
uvicorn>=0.32
pillow>=11.0
